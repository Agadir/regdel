source :gemcutter

gem 'rails', '= 3.1.0'

group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

gem 'sqlite3-ruby', :require => 'sqlite3'
gem 'haml'
gem 'sass'
gem 'nested_set'
gem 'state_machine'
gem 'inherited_resources'

gem 'jquery-rails'

gem "will_paginate"

group :test do
  gem "shoulda"
  gem 'machinist'
  gem 'web-app-theme'
end

gem 'vlad'
gem 'vlad-git'
