-if collection
  %table{:id => "entries-table"}
    %thead
      %tr
        %th Memo
        %th Date
        %th Amount
    %tbody
      -collection.each do |e|
        %tr
          %td= link_to e.memo, resource_path(e)
          %td= e.date.to_s
          %td= e.amount
