!!!
%html
  %head
    %title Regdel
    =render(:partial => 'layouts/head')
  %body
    #container
      =render(:partial => 'layouts/header') 
      #wrapper.wat-cf
        .flash
          - flash.each do |type, message|
            %div{:class => "message #{type}"}
              %p= message
        #main
          =render(:partial => 'layouts/ads') if Rails.env.demo?
          = yield
          =render(:partial => 'layouts/footer')
        #sidebar
          = yield :sidebar
