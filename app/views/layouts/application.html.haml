!!!
%html
  %head
    %title Regdel
    = stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/red/style", "web-app-theme/override", "regdel", :cache => true
    = javascript_include_tag :defaults, :cache => true
    = csrf_meta_tag
  %body
    #container
      =render(:partial => 'layouts/header')
      #wrapper.wat-cf
        .flash
          - flash.each do |type, message|
            %div{:class => "message #{type}"}
              %p= message
        #main
          = yield
          #footer
            .block
              %p
                Copyright &copy; #{Time.now.year} Regdel.
        #sidebar
          = yield :sidebar
