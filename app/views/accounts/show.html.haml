%h2= @account.name

%h3= @account.account_name

-if @sub_accounts
  %ul
  -@sub_accounts.each do |sub|
    %li= account_link_for(sub)

=render :partial => 'entries/entries_table', :locals => {:collection => @account.entries}

=link_to('Edit', edit_account_path(@account))
