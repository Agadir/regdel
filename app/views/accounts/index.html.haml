%h2 Accounts
-collection.group_by(&:type).each do |type, accounts|
  %h4.account_type=type
  %table
    -accounts.each do |a|
      %tr
        %td=link_to a.name, self.send("#{a.type.underscore}_path",a)
        %td=a.balance

=link_to "New", new_resource_path
